# คู่มือการใช้งานเครื่องมือแปลงและรันโมเดล AI บน NPU (ฉบับจับมือทำ)

คู่มือนี้เขียนขึ้นสำหรับ **"มือใหม่"** โดยเฉพาะ เพื่อให้คุณสามารถแปลงโมเดลจากคอมพิวเตอร์ (ONNX) ไปรันบนบอร์ดสมองกลฝังตัว (RK3588 NPU) ได้ตั้งแต่ต้นจนจบ

---

## 📋 สิ่งที่ต้องเตรียม (Checklist)

ก่อนเริ่มงาน ตรวจสอบว่าคุณมีของครบตามนี้หรือยัง:

1.  **ไฟล์โมเดล (.onnx):** ที่ Export มาจาก YOLOv5 หรือ YOLOv8
    *   *ข้อบังคับ:* ต้องมีขนาด Input **ตามขนาดที่ใช้เทรนโมเดล** (เช่น 640x640, 416x416, หรือขนาดอื่นๆ)
    *   *หมายเหตุ:* ถ้าใช้ขนาดอื่นที่ไม่ใช่ 640x640 อาจต้องแก้ไขค่าในโค้ดด้วย
    *   *ข้อบังคับ:* ต้องใช้ **Opset Version 12** (ตอน export จาก PyTorch)
2.  **รูปภาพทดสอบ (.jpg):** รูปอะไรก็ได้ที่เกี่ยวข้องกับงานของคุณ (เช่น รูปรถ, รูปป้ายทะเบียน)
3.  **โฟลเดอร์รูปภาพ (สำหรับโหมดโปร):** ถ้าจะทำ Quantization (INT8) ต้องมีโฟลเดอร์ที่มีรูปภาพประมาณ 50-100 รูป

---

## 🚀 ขั้นตอนที่ 1: การเตรียมไฟล์

เพื่อให้ไม่งง แนะนำให้จัดระเบียบไฟล์ดังนี้:

1.  สร้างโฟลเดอร์ชื่อ `my_models` (หรือชื่ออะไรก็ได้) ใน `Convert_tools/`
2.  เอาไฟล์ `.onnx` ของคุณไปวางไว้ในนั้น
3.  เอาไฟล์รูปภาพทดสอบ `test.jpg` มาวางไว้ที่ `Convert_tools/`

**โครงสร้างไฟล์ควรเป็นแบบนี้:**
```text
Convert_tools/
├── onnx_to_rknn_converter.py  <-- เครื่องมือพระเอกของเรา
├── npu_inference.py           <-- เครื่องมือทดสอบ
├── test.jpg                   <-- รูปที่จะเทส
└── my_models/
    └── best.onnx              <-- โมเดลของคุณ
```

---

## 🔄 ขั้นตอนที่ 2: การแปลงไฟล์ (ONNX -> RKNN)

เรามี 2 ทางเลือกครับ เลือกเอาตามความเหมาะสม:

### ทางเลือก A: แบบง่ายและเร็ว (FP16)
*เหมาะสำหรับ:* มือใหม่, ทดสอบโมเดลครั้งแรก, ไม่ซีเรียสเรื่องความเร็วสูงสุด
*ข้อดี:* ไม่ต้องเตรียมรูปเยอะ, แปลงเสร็จใน 10 วินาที

**คำสั่ง:**
```bash
python3 onnx_to_rknn_converter.py \
    --onnx my_models/best.onnx \
    --rknn my_models/best_fp16.rknn
```

### ทางเลือก B: แบบมือโปร (INT8 Quantization)
*เหมาะสำหรับ:* งานจริง (Production), ต้องการความเร็วสูงสุด (FPS สูงๆ)
*ข้อดี:* ไฟล์เล็กลงครึ่งนึง, รันบนบอร์ดเร็วขึ้นมาก
*ข้อเสีย:* ต้องมีโฟลเดอร์รูปภาพให้มันเรียนรู้ (Calibration)

**คำสั่ง:**
```bash
python3 onnx_to_rknn_converter.py \
    --onnx my_models/best.onnx \
    --rknn my_models/best_int8.rknn \
    --quantize \
    --images ./Data-set/  <-- ชี้ไปที่โฟลเดอร์รูปภาพของคุณ
```

---

## 📂 ขั้นตอนพิเศษ: การเตรียมรูปสำหรับโหมดโปร (Data-set)

ถ้าคุณเลือก **ทางเลือก B (INT8)** คุณต้องรู้จักโฟลเดอร์ `Data-set` ครับ

**มันคืออะไร?**
มันคือ "ห้องเรียนพิเศษ" ที่เราจะเอารูปภาพตัวอย่าง (Calibration Images) มาวางไว้ เพื่อสอนให้โมเดลรู้จักการย่อไฟล์โดยไม่ให้เสียความแม่นยำ

**วิธีใช้งาน:**
1.  **เตรียมรูป:** ไปก๊อปปี้รูปภาพจาก Dataset ที่คุณใช้เทรน (สัก 50-100 รูป) มาวางไว้ในโฟลเดอร์ `Convert_tools/Data-set/images/` (สร้างโฟลเดอร์ images ขึ้นมาใหม่ได้เลย)
2.  **สร้างใบรายชื่อ:** รันคำสั่งนี้เพื่อสร้างไฟล์ `dataset.txt` (ใบเช็คชื่อรูป):
    ```bash
    cd Data-set
    python3 create_dataset_txt.py
    cd ..
    ```
3.  **เอาไปใช้:** ตอนรันคำสั่งแปลงไฟล์ ให้ชี้ไปที่โฟลเดอร์นี้ตามตัวอย่างในข้อ B ครับ

---

## 🧪 ขั้นตอนที่ 3: การทดสอบ (Inference)

เมื่อได้ไฟล์ `.rknn` มาแล้ว (เช่น `best_fp16.rknn`) เราต้องมาลองรันดูว่ามันทำงานได้จริงไหม

**คำสั่ง:**
```bash
python3 npu_inference.py \
    --model my_models/best_fp16.rknn \
    --image test.jpg
```

**ถ้าสำเร็จ:**
1.  หน้าจอจะขึ้นว่า `🎉 Inference completed successfully!`
2.  มันจะบอกว่าเจออะไรบ้าง เช่น `Found 2 detections`
3.  รูปผลลัพธ์จะถูกเซฟไว้ในโฟลเดอร์ `npu_results/` (ลองเปิดดูรูปได้เลย)

**เทคนิคเพิ่มเติม:**
ถ้าโมเดลคุณมีหลาย Class (เช่น รถ, คน, หมา) ให้ระบุชื่อ Class ไปด้วย เพื่อให้มันโชว์ชื่อถูก:
```bash
python3 npu_inference.py \
    --model my_models/best_fp16.rknn \
    --image test.jpg \
    --classes car person dog
```

---

## ⚡ ขั้นตอนที่ 4: การแปลงทีละเยอะๆ (Batch Mode)

ถ้าคุณมีโมเดลเป็นสิบตัว ขี้เกียจพิมพ์คำสั่งทีละอัน ให้ใช้ตัวนี้ครับ

1.  เอาไฟล์ `.onnx` ทั้งหมดไปกองรวมกันในโฟลเดอร์นึง (เช่น `onnx_folder`)
2.  รันคำสั่งเดียวจบ:

```bash
python3 batch_onnx_converter.py \
    --onnx-dir ./onnx_folder \
    --output-dir ./rknn_folder
```

---

## ⚠️ ข้อควรระวัง (Do's and Don'ts)

### ✅ สิ่งที่ต้องทำ (Do)
*   **Input Size:** ต้องเช็คให้ชัวร์ว่าโมเดลรับภาพขนาดเท่าไหร่ **ตามขนาดที่ส่งรูปไปเทรนโมเดล** (เช่น 640x640, 416x416, 512x512 เป็นต้น) ถ้าขนาดไม่ตรงกัน โมเดลจะทำงานผิดพลาด **และถ้าใช้ขนาดอื่นที่ไม่ใช่ 640x640 จะต้องแก้ไขค่าในโค้ดด้วย**
*   **RGB:** รูปภาพต้องเป็น RGB เสมอ (เครื่องมือนี้จัดการให้แล้ว แต่ถ้ารันเองต้องระวัง)
*   **NPU Status:** ถ้ารันไม่ขึ้น ให้เช็คว่าเปิด NPU หรือยังด้วยคำสั่ง `sudo systemctl start rknn_server`

### ❌ สิ่งที่ห้ามทำ (Don't)
*   **Dynamic Shape:** ห้าม Export ONNX แบบขนาดไม่คงที่ (Dynamic) NPU ไม่ชอบครับ
*   **Opset ใหม่เกิน:** อย่าใช้ Opset 13, 14, 15... ให้ใช้ **Opset 12** ปลอดภัยสุด
*   **เปลี่ยนชื่อไฟล์มั่ว:** พยายามตั้งชื่อไฟล์ให้สื่อความหมาย เช่น `model_fp16.rknn` หรือ `model_int8.rknn` จะได้ไม่งงทีหลัง

---

## ❓ ถาม-ตอบ ปัญหาที่พบบ่อย

**Q: รันแล้วขึ้นว่า "Unsupported operator"**
A: แปลว่า ONNX ของคุณใช้คำสั่งแปลกๆ ที่ NPU ไม่รู้จัก -> **วิธีแก้:** กลับไป Export จาก PyTorch ใหม่ แล้วเลือก `opset=12`

**Q: รันแล้วไม่เจอวัตถุเลย (No detections)**
A: อาจจะเกิดจากค่าสี (Mean/Std) ผิด หรือขนาดภาพไม่ตรง -> **วิธีแก้:** ลองใช้คำสั่ง `--mean 0 0 0 --std 255 255 255` (ซึ่งเป็นค่า Default ของเครื่องมือนี้อยู่แล้ว)

**Q: เครื่องร้อนจี๋เลย**
A: ปกติครับ NPU ทำงานหนัก ให้หาพัดลมมาเป่า หรือลดความละเอียดภาพลง

---
*ขอให้สนุกกับการสร้าง AI ครับ! ถ้าติดตรงไหนให้กลับมาดูที่ "ขั้นตอนที่ 1" ใหม่ครับ* ✌️
